import { MatchPaswordValidatorDirective } from './match-pasword-validator.directive';

describe('MatchPaswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPaswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
