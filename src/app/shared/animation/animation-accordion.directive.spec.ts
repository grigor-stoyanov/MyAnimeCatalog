import { AnimationAccordionDirective } from './animation-accordion.directive';

describe('AnimationAccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationAccordionDirective();
    expect(directive).toBeTruthy();
  });
});
